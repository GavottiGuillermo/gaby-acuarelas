{
  "name": "gaby-acuarelas",
  "version": "0.1.0",
  "private": true,
  "description": "Tienda online de cursos y combos de acuarela.",
  "main": "src/server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "gate": "node scripts/check-stage-gate.js",
    "catalog:build": "node scripts/build-public-catalog.js",
    "catalog:check": "node scripts/check-course-catalog.js",
    "check": "node --check src/server.js && node --check public/js/app.js && node --check scripts/check-stage-gate.js && node --check scripts/build-public-catalog.js && node --check scripts/check-course-catalog.js && node scripts/check-stage-gate.js && node scripts/check-course-catalog.js"
  },
  "dependencies": {
    "dotenv": "^17.2.2",
    "express": "^5.1.0"
  }
}
